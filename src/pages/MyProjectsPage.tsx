import { MyProjectsView } from '../components/projects/MyProjectsView';

/**
 * Página que muestra los proyectos asignados al usuario autenticado.
 * Pensada para trabajo en campo por parte de evaluadores técnicos y sociales.
 */
export const MyProjectsPage = () => {
  return <MyProjectsView />;
};

export default MyProjectsPage;
